# wrap

Type: coding · Ownership: tool · Delivery mode: local-only

Lab-born skill: clean session exit with hang-status receipts
(`~/.local/state/session-wrap/`). Source of truth here; installed copies in
`~/.claude/skills`, `~/.hermes/skills`, `~/.codex/skills`. Follow the
Standards: `~/projects/.standards/`.
